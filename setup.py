from setuptools import setup, find_packages

setup(
    name="rag-video-transcript",
    version="0.1.0",
    description="RAG Video Transcription",
    packages=find_packages(),  # Ensure you have a package directory named 'extractor'
)
